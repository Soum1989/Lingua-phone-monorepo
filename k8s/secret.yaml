apiVersion: v1
kind: Secret
metadata:
  name: google-cloud-key
  namespace: lingua-app
type: Opaque
data:
  # The content should be the base64 encoded service account key
  # key.json: <base64-encoded-content>